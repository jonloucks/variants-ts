{
  "name": "@jonloucks/variants-ts-smoke",
  "version": "999.888.777",
  "description": "variants-ts smoke test package",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "tsc -p tsconfig.json --noEmit && tsx --test $(find src -name '*.test.ts')",
    "clean": "rm -rf dist && npm install",
    "compile": "tsc"
  },
  "author": "Jon Loucks",
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@jonloucks/contracts-ts": "^2.0.2"
  }
}
